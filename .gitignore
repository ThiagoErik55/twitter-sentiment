# ---------------------------------------------------
# 1. AMBIENTE PYTHON E DEPENDÊNCIAS
# ---------------------------------------------------
__pycache__/
*.pyc
*.pyd
*.so
.Python
# Arquivos de ambiente virtual
venv/
.venv/
env/
# Dependências instaladas (se você usa pip freeze)
*.egg-info/
dist/
build/

# ---------------------------------------------------
# 2. FERRAMENTAS E NOTEBOOKS
# ---------------------------------------------------
.ipynb_checkpoints  # Checkpoints temporários do Jupyter Notebook
.idea/              # Arquivos de projeto do PyCharm/IntelliJ
.vscode/            # Arquivos de configuração específicos do VS Code
Thumbs.db           # Cache de imagens do Windows
*.swp               # Arquivos temporários do Vim

# ---------------------------------------------------
# 3. DADOS BRUTOS, CACHE E SAÍDA DO PIPELINE (CRÍTICO!)
# ---------------------------------------------------
# A pasta 'data' que você mapeou no Docker/Airflow
# Contém dados brutos, bronze, silver, gold, que são grandes ou recriáveis.
data/
# Logs gerados pelo pipeline
*.log

# ---------------------------------------------------
# 4. AIRFLOW E DOCKER COMPOSE
# ---------------------------------------------------
# Pasta que armazena os logs do Airflow (muito grandes)
airflow/logs/
# Pasta onde o Airflow armazena o banco de dados interno e metadados
airflow/sqlite.db
airflow/webserver_*.pid
# Arquivos gerados pelo Docker Compose
docker-compose.yaml.Zone.Identifier # Arquivos de bloqueio do Windows/WSL
# Logs e caches gerados pelo Docker
.dockerignore

# ---------------------------------------------------
# 5. BANCO DE DADOS E SQLALCHEMY
# ---------------------------------------------------
# Arquivos de bancos de dados locais (SQLite)
*.db
*.sqlite
*.sqlite3

# ---------------------------------------------------
# 6. CONFIGURAÇÕES SECRETAS
# ---------------------------------------------------
# Arquivo que contém senhas do banco de dados e variáveis de ambiente
# O Airflow usa este arquivo para configuração
.env
# Arquivos de configuração do Docker Compose que podem ter senhas
docker-compose.override.yml